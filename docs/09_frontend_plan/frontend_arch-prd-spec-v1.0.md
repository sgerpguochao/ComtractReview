# 前端架构与 PRD 规格 v1.0

> 版本：v1.0 | 日期：2026-04-10
> 阶段：09_frontend_plan
> 作者：Frontend Architecture Team (Agent Teams)
> 前置文档：
> - `docs/08_api_spec/api_spec-v1.0.md`
> - `docs/07_data_model/data_model_spec-v1.0.md`
> - `docs/06_architecture/backend_design_final-v1.0.md`
> - `docs/06_architecture/frontend_backend_boundary_spec-v1.0.md`
> - `docs/04_interaction_design/interaction_core_final.md`
>
> 本文档是 09_frontend_plan 阶段的最终输出，汇总页面结构、组件模块、HITL 交互 UI 三大设计，作为前端实现的唯一依据。

---

## 文档索引

| 分文档 | 内容 | 文件 |
|--------|------|------|
| 页面结构与路由 | P1 首页 + P2 审查详情页结构、TaskStatus 面板映射、路由跳转、API 映射、SSE 降级 | [page_structure_upload_task.md](page_structure_upload_task.md) |
| 审核组件模块 | RiskItemList、RiskItemCard、ClauseLocator、ReviewOpinionPanel、ReviewSummaryCard、FilterBar | [component_modules_risk_review.md](component_modules_risk_review.md) |
| HITL 交互 UI | Approve/Edit/Reject 操作 UI、批量操作、操作反馈、审核会话管理、操作日志 | [hitl_interaction_ui_design.md](hitl_interaction_ui_design.md) |

---

## 1. 页面层级结构

### 1.1 路由树

```
/                           (P1 首页/工作台)
├── /upload                 (上传入口 — 首页内嵌，非独立路由)
├── /tasks                  (最近审查记录列表 — 首页内嵌)
│
└── /review/:task_id        (P2 审查详情页)
    ├── /review/:task_id/parsing        (解析中 — 逻辑面板)
    ├── /review/:task_id/reviewing      (AI 审查中 — 逻辑面板)
    ├── /review/:task_id/failed         (失败 — 逻辑面板)
    ├── /review/:task_id/pending-review (待人工审核 — 逻辑面板)
    └── /review/:task_id/report-ready   (报告完成 — 逻辑面板)
    (注：子路由为逻辑面板，不映射到独立 URL，由 :task_id 页面根据 TaskStatus 动态渲染)
```

### 1.2 路由策略

审查详情页使用单一路由 `/review/:task_id`，页面内部根据 TaskStatus 动态渲染不同面板：
- 状态由后端驱动实时变化，频繁路由切换体验差
- 同一 task_id 下状态流转是线性的，用户始终在同一页面操作
- 便于保持 SSE 连接的生命周期

### 1.3 页面跳转关系

| 起点 | 终点 | 触发条件 | 数据传递 |
|------|------|---------|---------|
| P1 `/` | P2 `/review/:task_id` | 上传成功，返回 task_id | URL 参数 task_id |
| P1 `/` | P2 `/review/:task_id` | 点击最近审查记录列表中的任务 | URL 参数 task_id |
| P2 `/review/:task_id` | P1 `/` | 点击返回按钮 / 重新上传 | 无（P1 自动刷新列表） |
| P2 `/review/:task_id` | P3 报告预览页 | 报告完成面板点击「查看报告」 | URL 参数 task_id（后续阶段设计） |
| P2 内部面板切换 | P2 自身 | SSE 状态变更事件 | 内部状态更新，不跳转路由 |

---

## 2. 页面线框图

### 2.1 P1 首页 / 工作台

```
┌─────────────────────────────────────────────────────────────────┐
│  ContractReview                          [导航栏]                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  上传区域 (UploadZone)                                    │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │              拖拽文件到此处，或 点击上传              │  │  │
│  │  │              支持 .docx / .pdf，最大 50MB            │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  □ 风险审查    □ 合规检查      [开始审查]                  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  最近审查记录 (RecentTasks)                               │  │
│  │  ─────────────────────────────────────────────────────    │  │
│  │  采购合同_2026Q2.docx    报告可导出    25项风险   04-10    │  │
│  │  服务合同_draft.pdf      AI审查中      ▓▓▓░░ 72%   04-09   │  │
│  │  租赁合同.docx           解析失败      重试       04-08    │  │
│  │                              [加载更多]                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 P2 审查详情页

```
┌─────────────────────────────────────────────────────────────────┐
│  ContractReview     ← 返回  |  采购合同_2026Q2.docx   [操作日志] │
├─────────────────────────────────────────────────────────────────┤
│  文件信息 (FileInfo)                                            │
│  大小: 5.0 MB  |  上传时间: 2026-04-10 06:30  |  状态: [标签]  │
│                                           [取消审查] 按钮       │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  状态面板 (StatusPanel) — 根据 TaskStatus 动态渲染         │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─ 解析中/审查中面板 ──────────────────────────────────────┐  │
│  │  ① 文档解析      ▓▓▓▓▓▓░░░░  60%                        │  │
│  │  ② 条款提取      ░░░░░░░░░░  等待                       │  │
│  │  ③ 风险识别      ░░░░░░░░░░  等待                       │  │
│  │  ④ 合规检查      ░░░░░░░░░░  等待                       │  │
│  │  总进度 ████████████████████░░░░░░░░░░  65%             │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─ 待人工审核面板 ─────────────────────────────────────────┐  │
│  │  ┌─ 高风险 (5项) ────────────────────────────────────┐   │  │
│  │  │ ▼ 风险卡片1 [待审核]  违约责任  置信度92%          │   │  │
│  │  │ ▼ 风险卡片2 [已确认]  知识产权  置信度88%          │   │  │
│  │  └───────────────────────────────────────────────────┘   │  │
│  │  ┌─ 中风险 (8项) ────────────────────────────────────┐   │  │
│  │  │ ▶ ...                                             │   │  │
│  │  └───────────────────────────────────────────────────┘   │  │
│  │  ┌─ 低风险 (12项) ───────────────────────────────────┐   │  │
│  │  │ ▶ ...                                             │   │  │
│  │  └───────────────────────────────────────────────────┘   │  │
│  │  剩余待处理: 3 项        [批量确认] [生成报告] (未激活)   │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─ 报告完成面板 ───────────────────────────────────────────┐  │
│  │  审查完成                                                 │  │
│  │  共 25 项风险，经人工审核已处理全部高风险项               │  │
│  │         [查看报告]              [下载 PDF 报告]           │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 组件树总览

```
App
 └── HomePage (/)
      ├── Header
      ├── UploadZone
      │    ├── DropZone
      │    ├── FileSelector
      │    ├── ReviewDimensionSelector
      │    └── UploadButton
      ├── UploadProgress (条件渲染)
      ├── RecentTasks
      │    ├── TaskListItem[]
      │    └── LoadMoreButton
      └── ErrorToast (条件渲染)
 │
 └── ReviewPage (/review/:task_id)
      ├── Header (Logo + 返回 + 文件名 + 操作日志入口)
      ├── FileInfo (文件名 + 大小 + 时间 + 状态标签 + 取消按钮)
      │
      ├── StatusPanel (根据 TaskStatus 选择)
      │    ├── ParsingPanel
      │    │    ├── StageList
      │    │    └── TotalProgressBar
      │    ├── ReviewingPanel
      │    │    ├── StageList
      │    │    └── TotalProgressBar
      │    ├── ParseFailedPanel
      │    ├── ReviewFailedPanel
      │    ├── PendingReviewPanel
      │    │    ├── ReviewSummaryCard
      │    │    │    ├── RiskScoreDisplay
      │    │    │    ├── RiskCountCards
      │    │    │    └── ReportActionSection
      │    │    ├── FilterBar
      │    │    │    ├── LevelFilterGroup
      │    │    │    ├── StatusFilterGroup
      │    │    │    ├── SortSelector
      │    │    │    └── BatchActionButtons
      │    │    ├── RiskItemList
      │    │    │    ├── GroupHeader[] (high/medium/low)
      │    │    │    └── RiskItemCard[]
      │    │    │         ├── ClauseLocator (展开态)
      │    │    │         ├── ReviewOpinionPanel (展开态)
      │    │    │         └── ReviewActionBar (展开态)
      │    │    ├── RemainingCounter
      │    │    └── GenerateReportButton
      │    └── ReportReadyPanel
      │         ├── SummaryText
      │         ├── ViewReportButton
      │         └── DownloadReportButton
      │
      └── AuditLogDrawer (条件渲染)
           └── AuditLogTimeline
```

---

## 4. TaskStatus → UI 面板映射

| TaskStatus | 渲染面板 | 按钮可见性 | SSE 连接 | 说明 |
|------------|---------|-----------|---------|------|
| `uploaded` | FileInfo（仅文件信息） | 显示「取消审查」 | 建立连接，等待状态变更 | 文件已上传，后端尚未开始解析 |
| `parsing` | ParsingPanel | 显示「取消审查」 | 建立连接，监听进度事件 | 4 阶段列表高亮当前阶段，显示总进度条 |
| `parse_complete` | ParsingPanel（阶段 1 标记完成） | 显示「取消审查」 | 保持连接，等待进入 reviewing | 解析完成，即将进入 AI 审查 |
| `parse_failed` | ParseFailedPanel | 显示「重新上传」「取消」 | 断开 | 展示失败原因，用户可重新上传或取消 |
| `reviewing` | ReviewingPanel | 显示「取消审查」 | 建立/保持连接，监听 4 阶段进度 | 4 阶段逐项高亮，总进度条更新 |
| `review_failed` | ReviewFailedPanel | 显示「重试」「取消」 | 断开 | 展示失败原因，支持断点重试 |
| `pending_review` | PendingReviewPanel | 显示「批量确认」「生成报告」(remaining=0 激活) | 断开 | 加载风险项列表，按等级分组展示 |
| `human_reviewing` | PendingReviewPanel | 显示「批量确认」「生成报告」(remaining=0 激活) | 断开 | 与 pending_review 相同面板，状态标签不同 |
| `report_ready` | ReportReadyPanel | 显示「查看报告」「下载 PDF 报告」 | 断开 | 展示审查总结，提供报告查看/下载入口 |
| `cancelled` | FileInfo + 已取消提示 | 显示「重新上传」 | 断开 | 文件信息 + 已取消状态，可重新发起 |

---

## 5. 审核组件模块

### 5.1 组件层级关系

```
ReviewSummaryCard（审核结果总览）
├── FilterBar（筛选排序栏）
│   └── 批量操作按钮
└── RiskItemList（风险项列表）
    ├── GroupHeader（分组头部）
    └── RiskItemCard（风险项卡片，可展开/收起）
        ├── ClauseLocator（原文定位）— 左栏
        ├── ReviewOpinionPanel（审查意见）— 右栏
        └── ReviewActionBar（审核操作栏）— 底部入口
```

### 5.2 风险项卡片 — 收起态

```
┌─────────────────────────────────────────────────────────────┐
│ [高] 单方无限制解除权，违反公平原则                          │
│      第八条·第2款 · 第5页    置信度 ████████░░ 92%          │
│                              [待处理 ▼]              [展开 ▶]│
└─────────────────────────────────────────────────────────────┘
```

已处理态（视觉弱化，60% 透明度）：
```
┌─────────────────────────────────────────────────────────────┐
│ [高] 单方无限制解除权，违反公平原则                          │
│      第八条·第2款 · 第5页    置信度 ████████░░ 92%          │
│                              [已确认 ✓]              [展开 ▶]│
└─────────────────────────────────────────────────────────────┘
```

### 5.3 风险项卡片 — 展开态

```
┌─────────────────────────────────────────────────────────────────┐
│ [高] 单方无限制解除权，违反公平原则                              │
│      第八条·第2款 · 第5页    置信度 ████████░░ 92%              │
│      分类: 合同解除           [待处理 ▼]                  [收起 ▼]│
├───────────────────────┬─────────────────────────────────────────┤
│  原文定位              │  审查意见                                │
│                       │                                          │
│  ┌─────────────────┐  │  风险描述：                              │
│  │ 第八条 合同解除   │  │  单方无限制解除权...                    │
│  │ 甲方有权单方面   │  │  ─────────────────────────────────     │
│  │ 解除本合同...    │  │  法律依据：                              │
│  │ (黄色底色高亮)    │  │  《民法典》第五百六十三条...  [展开]    │
│  └─────────────────┘  │  ─────────────────────────────────     │
│  页码: 5              │  修改建议：                              │
│  偏移: 1024-1156      │  建议增加双方协商解除条款...   [复制]    │
│                       │  ─────────────────────────────────     │
│                       │  AI 置信度: 92%                         │
├───────────────────────┴─────────────────────────────────────────┤
│ [同意]    [修改]    [驳回]                                      │
└─────────────────────────────────────────────────────────────────┘
```

### 5.4 原文定位组件（ClauseLocator）

```
┌─────────────────────────────────┐
│ 原文定位                         │
├─────────────────────────────────┤
│ 面包屑: 第八条 > 第2款 · 第5页  │
├─────────────────────────────────┤
│  ...前序条款文本...              │
│  ┌───────────────────────────┐  │
│  │ 甲方有权单方面解除本合同，  │  │
│  │ 无需承担任何违约责任。      │  │
│  │ (黄色底色 #FEF9C3 高亮)    │  │
│  └───────────────────────────┘  │
│  ...后续条款文本...              │
├─────────────────────────────────┤
│ 位置: 第5页 | 偏移: 1024-1156   │
└─────────────────────────────────┘
```

### 5.5 审查意见组件（ReviewOpinionPanel）

```
┌──────────────────────────────────────────────┐
│ 审查意见                                      │
├──────────────────────────────────────────────┤
│  风险描述                                     │
│  单方无限制解除权，违反公平原则...             │
│ ─────────────────────────────────────────── │
│  法律依据                              [展开 ▶]│
│  《中华人民共和国民法典》第五百六十三条：      │
│  有下列情形之一的，当事人可以解除合同：        │
│  (收起时最多展示 2 行，超出省略号截断)          │
│ ─────────────────────────────────────────── │
│  修改建议                              [复制]  │
│  建议增加双方协商解除条款...                   │
│ ─────────────────────────────────────────── │
│  AI 置信度                                    │
│  ████████░░ 92%（高）                         │
└──────────────────────────────────────────────┘
```

### 5.6 筛选排序栏（FilterBar）

```
┌─────────────────────────────────────────────────────────────────┐
│ 风险等级: [●全部] [  高  ] [  中  ] [  低  ]                    │
│ 审核状态: [●全部] [ 待处理 ] [ 已处理 ]                          │
│ 排序: [置信度 降序 ▼]    共 25 项    [批量确认] [批量驳回]        │
└─────────────────────────────────────────────────────────────────┘
```

### 5.7 审核结果总览（ReviewSummaryCard）

```
┌───────────────────────────────────────────────────────────────┐
│  审查结果总览                                                   │
│  总体风险评分: ┌─────────┐                                     │
│                ║  72/100 ║  中等风险                            │
│                └─────────┘                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                    │
│  │ ●  5 高风险│  │ ●  8 中风险│  │ ●  12 低风险│                    │
│  └──────────┘  └──────────┘  └──────────┘                    │
│  待处理高风险项: 3                     [生成审查报告]            │
└───────────────────────────────────────────────────────────────┘
```

### 5.8 关键样式令牌

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| `color-risk-high` | `#DC2626` | 高风险标签/边框 |
| `color-risk-high-bg` | `#FEF2F2` | 高风险标签背景 |
| `color-risk-medium` | `#D97706` | 中风险标签/边框 |
| `color-risk-medium-bg` | `#FFFBEB` | 中风险标签背景 |
| `color-risk-low` | `#2563EB` | 低风险标签/边框 |
| `color-risk-low-bg` | `#EFF6FF` | 低风险标签背景 |
| `color-highlight-bg` | `#FEF9C3` | 原文高亮底色 |
| `color-highlight-keyword` | `#DC2626` | 关键风险词下划线色 |
| `color-processed-dim` | `0.6` opacity | 已处理卡片透明度 |

### 5.9 审核状态标签映射

| human_review_status | 标签文本 | 背景色 | 文字色 |
|---------------------|---------|--------|--------|
| `pending` | "待处理" | `#F3F4F6` | `#6B7280` |
| `approved` | "已确认" | `#F0FDF4` | `#16A34A` |
| `modified` | "已修改" | `#FFFBEB` | `#D97706` |
| `rejected` | "已驳回" | `#FEF2F2` | `#DC2626` |

### 5.10 规则分类中文映射

| category | 中文标签 |
|----------|---------|
| `breach_of_contract` | 违约责任 |
| `intellectual_property` | 知识产权 |
| `confidentiality` | 保密条款 |
| `dispute_resolution` | 争议解决 |
| `contract_termination` | 合同解除 |
| `force_majeure` | 不可抗力 |
| `data_compliance` | 数据合规 |
| `payment_terms` | 付款条款 |
| `liability_limitation` | 责任限制 |
| `other` | 其他 |

---

## 6. HITL 人机交互 UI

### 6.1 审批操作 — Approve（确认）

**置信度阈值决策**：

| 置信度 | 确认方式 |
|--------|----------|
| >= 70% | **内联直接确认**（300ms 动画 → Toast → 卡片弱化） |
| < 70%  | **弹出确认对话框**（居中 + 半透明遮罩） |

**高置信度完成态**：
```
┌─────────────────────────────────────────────────────────────┐
│ [高] 风险 #001 - 违约责任                      [已确认] [展开▼]│
│ 置信度: 92%  操作人: user-001  2026-04-10 07:00              │
│ 透明度: 60% | 默认折叠                                        │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 审批操作 — Edit（修改）

480px 右侧滑出抽屉面板，包含：

```
+----------------------------------------------------------+
|  修正风险 #001                                      [X]  |
+----------------------------------------------------------+
|  风险等级调整                                              |
|  [o] 保持原等级 [HIGH] (红色)                              |
|  [ ] 降为中风险 [MEDIUM] (橙色)                            |
|  [ ] 降为低风险 [LOW] (黄色)                               |
|                                                          |
|  审查意见（预填 AI 原意见，可编辑，最多 500 字）             |
|  修改建议（预填 AI 原建议，可编辑，最多 500 字）             |
|  法律依据（预填 AI 原依据，可编辑，最多 500 字）             |
+----------------------------------------------------------+
|  [ 取消 ]                              [ 保存修改 ]        |
+----------------------------------------------------------+
```

- 保存校验：至少一个字段与原始值不同，否则按钮禁用
- 完成后：卡片显示"已修正" + diff 风格变更对比

### 6.3 审批操作 — Reject（驳回）

卡片底部内联展开驳回面板：

```
+--------------------------------------------------------------+
|  驳回原因（必选）                                               |
|  ( ) 误报 - AI 识别错误，该条款无风险                          |
|  ( ) 不适用 - 该条款对本合同类型不适用                         |
|  ( ) 已修正 - 合同已更新，该风险已消除                         |
|  ( ) 其他 - 其他原因                                           |
|                                                              |
|  补充说明（选填，最多 200 字）                                  |
|                    [ 取消 ]  [ 确认驳回 ]                      |
+--------------------------------------------------------------+
```

### 6.4 批量操作

**选中态**：风险项列表左侧 checkbox，选中项左侧有 3px 蓝色竖线

**底部浮动工具栏**（选中 >= 1 项时滑出）：
```
+----------------------------------------------------------+
|  [ ] 全选    已选择 2 项                                    |
|     [ 批量确认 ]                    [ 批量驳回 ]            |
+----------------------------------------------------------+
```

**批量驳回弹窗**（居中对话框）：与单条驳回相同的 4 个预设原因选项。

### 6.5 操作反馈规范

| 操作 | Toast 文案 | 背景色 | 持续时间 |
|------|-----------|--------|----------|
| 单条确认 | `已确认` | 绿 `#E8F5E9` | 1.5s |
| 单条修改 | `已修正` + 变更标记 | 橙 `#FFF3E0` | 3s |
| 单条驳回 | `已驳回（误报）` | 红 `#FFEBEE` | 1.5s |
| 批量确认 | `已确认 3 项` | 绿 `#E8F5E9` | 2s |
| 批量驳回 | `已驳回 3 项（误报）` | 红 `#FFEBEE` | 2s |

**已处理项视觉规范**：
- 透明度 `opacity: 0.6`
- 默认折叠，仅显示摘要行
- 操作按钮隐藏，替换为状态标签

### 6.6 审核会话管理 UI

```
┌─────────────────────────────────────────────────────────────┐
│ 审核会话 #1 (第1轮)    剩余: 5 高 | 8 中 | 12 低              │
│ 状态: [审核中] (绿色)  超时: 23:45:12                        │
│ 进度: ████████░░░░░░░░░░░░  25/25 已完成                     │
└─────────────────────────────────────────────────────────────┘
```

**超时警告梯度**：

| 剩余时间 | 行为 |
|----------|------|
| > 1 小时 | 正常显示倒计时（绿色文字） |
| 30 分钟 ~ 1 小时 | 倒计时变橙色 |
| < 30 分钟 | 内嵌橙色警告条 |
| < 5 分钟 | 红色警告 + 每 60s 弹确认弹窗 |
| 已超时 | 禁用所有操作 + Toast 提示 |

### 6.7 操作日志抽屉

400px 右侧滑出，时间线布局：

```
+--------------------------------------------------+
|  操作日志                                   [X]  |
+--------------------------------------------------+
|  2026-04-10                                       |
|  07:30  [文档]  任务状态变更 → report_ready       |
|  07:28  [保存]  生成审查报告                       |
|  07:15  [对勾]  批量确认 3 项风险                  |
|  07:10  [叉号]  驳回风险项 risk-002 (误报)        |
|  07:05  [铅笔]  修正风险项 risk-001 (HIGH→MEDIUM)  |
|  07:00  [对勾]  确认风险项 risk-006               |
|  06:50  [闪电]  AI 审查完成，待人工审核            |
|  06:45  [开始]  开始人工审核（第1轮）              |
+--------------------------------------------------+
```

### 6.8 前端状态管理建议

**全局 Store 结构**：
```
ReviewState {
  session: { sessionNumber, totalRisks, decisionsCount, status, timeoutAt }
  items: RiskItem[]
  selectedIds: Set<string>
  remainingPending: number
  pendingOperations: Map<risk_id, { action, status }>
}
```

**乐观更新策略**：操作成功后立即更新 UI，API 返回错误时回滚。

---

## 7. API 映射总览

### 7.1 P1 首页

| API | 方法 | 路径 | 状态 | 调用时机 |
|-----|------|------|------|---------|
| 任务列表查询 | GET | `/api/v1/tasks` | **已定义** | 页面加载时获取最近审查记录 |
| 文档上传 | POST | `/api/v1/tasks/upload` | **已定义** | 用户选择文件后点击「开始审查」 |

**未开发接口**：无。

### 7.2 P2 审查详情页

| API | 方法 | 路径 | 状态 | 调用时机 |
|-----|------|------|------|---------|
| 任务状态查询 | GET | `/api/v1/tasks/{task_id}` | **已定义** | 页面加载 + SSE 降级轮询（3s/次） |
| SSE 进度推送 | GET | `/api/v1/tasks/{task_id}/stream` | **已定义** | 进入页面后建立连接 |
| 取消任务 | POST | `/api/v1/tasks/{task_id}/cancel` | **已定义** | 用户点击「取消审查」 |
| 审查结果查询 | GET | `/api/v1/tasks/{task_id}/result` | **已定义** | 收到 SSE `review_pending` 事件后加载 |
| 风险项详情 | GET | `/api/v1/tasks/{task_id}/risks/{risk_id}` | **已定义** | 用户展开风险卡片时 |
| 提交人工审核（单条） | PUT | `/api/v1/tasks/{task_id}/risks/{risk_id}/review` | **已定义** | 用户点击同意/编辑/驳回 |
| 批量人工审核 | POST | `/api/v1/tasks/{task_id}/reviews/batch` | **已定义** | 用户批量确认/驳回 |
| 生成审查报告 | POST | `/api/v1/tasks/{task_id}/report/generate` | **已定义** | 全部高风险处理后 |
| 下载报告 | GET | `/api/v1/tasks/{task_id}/report` | **已定义** | 用户点击「下载 PDF 报告」 |
| 查询操作日志 | GET | `/api/v1/tasks/{task_id}/auditlog` | **已定义** | 用户点击操作日志入口 |

**未开发接口**：无。P2 所需全部接口均在 `api_spec-v1.0.md` 中完整定义。

---

## 8. 页面初始化数据加载流程

### 8.1 P1 首页加载

```
页面挂载
   ├─→ GET /api/v1/tasks?limit=10
   │    ├─ 成功 → 渲染 RecentTasks 列表
   │    │         └─ 点击任一任务 → navigate(`/review/${task.id}`)
   │    └─ 失败 → 显示空状态 + 错误提示
   └─→ 等待用户操作（上传文件）
```

### 8.2 P2 审查详情页加载

```
从 URL 提取 task_id
   ├─→ GET /api/v1/tasks/{task_id}
   │    ├─ 200 → 拿到 status, progress, current_stage 等
   │    │    ├─ 渲染 FileInfo（始终显示）
   │    │    └─ 根据 status 决定初始面板：
   │    │         ├─ uploaded/parsing/parse_complete/reviewing
   │    │         │    └─ 连接 SSE
   │    │         │         ├─ status_change → 更新面板
   │    │         │         ├─ review_pending → 断开 SSE，加载风险列表
   │    │         │         ├─ error → 失败面板
   │    │         │         └─ completed → 报告完成面板
   │    │         ├─ pending_review/human_reviewing → 加载风险列表
   │    │         ├─ parse_failed/review_failed → 失败面板
   │    │         ├─ report_ready → 报告完成面板
   │    │         └─ cancelled → 已取消提示
   │    └─ 404 → 跳转 P1 + Toast「任务不存在」
   └─→ 等待用户操作或 SSE 事件
```

### 8.3 SSE 降级轮询

```
SSE 连接失败/超时 → 降级为轮询
   └─ 每 3 秒: GET /api/v1/tasks/{task_id}
        ├─ 状态变化 → 更新面板
        │    ├─ pending_review → 停止轮询，加载风险列表
        │    ├─ failed → 停止轮询，渲染失败面板
        │    └─ report_ready → 停止轮询，渲染完成面板
        └─ 状态未变 → 继续轮询
```

---

## 9. 风险项分组渲染规则

```
RiskList
 ├── 高风险组 (level === 'high') — 默认展开
 │    ├── 组头：「高风险 (5项)」— 红色标签
 │    └── RiskCard[] — 按 confidence 降序排列
 ├── 中风险组 (level === 'medium') — 默认收起
 │    ├── 组头：「中风险 (8项)」— 橙色标签
 │    └── RiskCard[]
 └── 低风险组 (level === 'low') — 默认收起
      ├── 组头：「低风险 (12项)」— 蓝色标签
      └── RiskCard[]
```

- 已处理项（approved/modified/rejected）视觉弱化（60% 透明度，默认折叠）
- 「生成报告」按钮在 `remaining_pending === 0` 时激活
- 风险评分前端本地计算：`score = (high_count * 10 + medium_count * 5 + low_count * 2)`，上限 100

---

## 10. 上传交互流程

1. 用户拖拽或点击选择文件
2. 前端客户端校验：扩展名（.docx/.pdf）、大小（≤ 50MB）
3. 校验失败 → 原地显示错误提示，不发起请求
4. 校验通过 → 显示上传进度条，禁用上传区域
5. `POST /api/v1/tasks/upload`（multipart/form-data）
6. 成功返回 task_id → `navigate('/review/{task_id}')`
7. 失败 → Toast 提示错误码，恢复上传区域

---

## 11. 组件清单汇总

| 组件名 | 职责 | 所属模块 |
|--------|------|----------|
| `Header` | 顶栏导航 | P1/P2 通用 |
| `UploadZone` | 文件上传区域（拖拽 + 点击） | P1 |
| `UploadProgress` | 上传进度条 | P1 |
| `RecentTasks` | 最近审查记录列表 | P1 |
| `TaskListItem` | 单条任务记录展示 | P1 |
| `FileInfo` | 文件信息栏 | P2 |
| `ParsingPanel` | 解析中进度面板 | P2 |
| `ReviewingPanel` | AI 审查中进度面板 | P2 |
| `ParseFailedPanel` | 解析失败面板 | P2 |
| `ReviewFailedPanel` | 审查失败面板 | P2 |
| `PendingReviewPanel` | 待人工审核面板 | P2 |
| `ReportReadyPanel` | 报告完成面板 | P2 |
| `ReviewSummaryCard` | 审核结果总览 | P2 |
| `FilterBar` | 筛选排序栏 | P2 |
| `RiskItemList` | 风险项分组列表 | P2 |
| `GroupHeader` | 分组头部 | P2 |
| `RiskItemCard` | 风险项卡片 | P2 |
| `ClauseLocator` | 原文定位 | P2 |
| `ReviewOpinionPanel` | 审查意见展示 | P2 |
| `ReviewActionBar` | 审核操作入口 | P2 |
| `ConfirmDialog` | 低置信度确认对话框 | HITL |
| `EditDrawer` | 修改抽屉面板（480px） | HITL |
| `RejectPanel` | 内联驳回面板 | HITL |
| `BatchToolbar` | 底部浮动批量工具栏 | HITL |
| `BatchRejectDialog` | 批量驳回对话框 | HITL |
| `StatusBadge` | 状态标签 | HITL |
| `SessionHeader` | 审核会话状态栏 | HITL |
| `TimeoutWarning` | 超时警告条 | HITL |
| `LogDrawer` | 操作日志抽屉（400px） | HITL |
| `TimelineItem` | 日志时间线单项 | HITL |
| `AuditLogDrawer` | 侧边日志抽屉 | P2 |
| `Toast` | 全局 Toast 提示 | 全局 |

---

## 12. 下一步

1. **09_frontend_plan 后续**：基于本 PRD 规格，制定技术栈选型、项目结构、构建配置
2. **10_backend_plan**：后端实现计划（与前端并行开发）
3. **联调阶段**：按 `api_spec-v1.0.md` 第 5 节的 6 个 Phase 顺序联调